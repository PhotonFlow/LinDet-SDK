# ── Camera Configuration ─────────────────────────────────────────────────────
# Source types: csi, usb, rtsp, test
camera:
  source_type: "test"
  device: "/dev/video0"
  width: 1280
  height: 720
  fps: 30
  frame_id: "camera_optical_frame"
